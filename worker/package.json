{
  "name": "screenshare-signaling",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object signaling hub for screenshare",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.20",
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.0.0"
  }
}
